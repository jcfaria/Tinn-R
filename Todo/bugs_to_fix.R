#. R highlighter and quotes
test <- "testing without quotes"; message(test)

# Single quote
test2 <- 'testing with three \"\"\" quote characters'; message(test2)

# Double quote
test3 <- "testing with three \"\"\" quote characters"; message(test2)

#. Marc Laurencelle (2020/19/05)
co2 "\\" CO2
iris

#. Using double cote
CO2 "\\" BOD
CO2 "\\a" BOD
CO2 "\\\" BOD
CO2 "\\\a" BOD

#. Using single cote
CO2 "/" BOD
CO2 "//" BOD